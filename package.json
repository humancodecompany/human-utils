{
  "name": "human-utils",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm run lint",
    "preversion": "npm run lint",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/humancodecompany/human-utils.git"
  },
  "keywords": ["utils", "human code", "human code company"],
  "author": "human code co.",
  "license": "ISC",
  "homepage": "https://github.com/humancodecompany/human-utils.git#readme",
  "dependencies": {
    "cpf-cnpj-validator": "^1.0.3",
    "date-fns": "^2.19.0"
  },
  "files": ["lib/**/*"],
  "devDependencies": {
    "prettier": "^2.2.1",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.2.3"
  }
}
